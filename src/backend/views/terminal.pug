html
    head
        title Hello
        script(src="/static/scripts/xterm.js")
        script(src="/static/scripts/xterm-addon-fit.js")
        script(src="/static/scripts/terminal.js")
        link(rel="stylesheet", href="/static/styles/xterm.css")
        link(rel="stylesheet", href="/static/styles/terminal.css")

    body
        #terminal

        script.
            var terminal = new Terminal();
            var fitAddon = new FitAddon.FitAddon();
            terminal.open(document.getElementById("terminal"));
            terminal.loadAddon(fitAddon);
            terminal.write("Hello from \\x1B[1;3;31mxterm.js\x1B[0m $ ");